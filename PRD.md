# PRD: YouTube 댓글 분석 웹앱 (국어 수업용)
**버전:** 2.0
**최종 수정:** 2026-03-13
**상태:** 확정

---

## 1. 프로젝트 개요

### 1.1 목적
중·고등학교 국어 수업에서 학생들이 YouTube 영상 댓글을 수집·분석하여 실생활 언어 데이터를 탐구하는 웹 기반 도구를 제공한다.
학생 PC에 **별도 설치 없이** 브라우저만으로 모든 기능을 실행할 수 있어야 한다.

### 1.2 핵심 설계 원칙
| 원칙 | 내용 |
|------|------|
| **설치 불필요** | Python, Node.js 등 런타임 설치 없이 URL 접속만으로 사용 |
| **백엔드 없음** | 서버 없이 순수 브라우저(JavaScript)에서 모든 처리 수행 |
| **API 키 학생 직접 발급** | YouTube Data API v3 키를 학생이 직접 발급·입력 |
| **한국어 특화 전처리** | 형태소 분석기 없이 정규식 기반 조사 제거로 의미 있는 빈도 분석 |
| **무료 배포** | GitHub Pages 정적 호스팅 |

### 1.3 타겟 사용자
- **학생:** YouTube API 키를 발급받아 직접 댓글 수집 및 분석
- **교사:** 수업 설계, 분석 결과 해설 및 토론 진행

### 1.4 배포 환경
- URL 하나로 접속 (GitHub Pages)
- 지원 브라우저: Chrome, Edge (최신 버전)
- 지원 OS: Windows 10/11, macOS, 모바일(기본 기능)

---

## 2. 기술 스택

| 구분 | 기술 | 선택 이유 |
|------|------|-----------|
| UI 프레임워크 | Vanilla JavaScript + HTML/CSS | 빌드 환경 불필요, GitHub Pages 바로 배포 |
| 스타일 | Tailwind CSS (CDN) | 별도 빌드 없이 CDN으로 사용 |
| YouTube 댓글 수집 | YouTube Data API v3 (fetch) | 브라우저에서 직접 REST 호출 가능, CORS 지원 |
| 한국어 전처리 | 정규식 (Vanilla JS) | 백엔드 없이 브라우저에서 조사 제거 |
| 막대차트 | Chart.js (CDN) | 경량, 한국어 폰트 지원 |
| 워드클라우드 | wordcloud2.js (CDN) | Canvas 기반, 한국어 지원 |
| SNA 네트워크 | D3.js force simulation (CDN) | 인터랙티브 그래프, 순수 JS |
| 폰트 | Nanum Gothic (Google Fonts CDN) | 한글 깨짐 없는 시각화 |
| 호스팅 | GitHub Pages | 무료, 정적 파일 배포 |

---

## 3. 아키텍처

```
┌─────────────────────────────────────────┐
│            학생 브라우저                 │
│                                         │
│  ┌─────────────┐    ┌────────────────┐  │
│  │  index.html  │    │  YouTube API   │  │
│  │  (SPA)       │◄──►│  v3 (REST)     │  │
│  │             │    │  (외부 서버)    │  │
│  │  JS modules  │    └────────────────┘  │
│  │  - crawler   │                        │
│  │  - preprocess│  데이터 흐름:           │
│  │  - analyzer  │  YouTube API → JS 처리  │
│  │  - charts    │  → 브라우저 내 시각화   │
│  │  - wordcloud │                        │
│  │  - sna       │  모든 데이터는 브라우저  │
│  └─────────────┘  메모리에만 존재         │
│                                         │
└─────────────────────────────────────────┘
         ↑ GitHub Pages에서 정적 파일 서빙
```

**데이터 흐름:**
1. 학생이 YouTube API 키 입력 → `localStorage` 저장
2. 영상 URL 입력 → 브라우저에서 YouTube API 직접 호출
3. 수집된 댓글 → 브라우저 메모리(JS 배열)에 저장
4. 전처리·분석·시각화 → 모두 브라우저 내 JS 처리
5. 결과 다운로드 → 브라우저 Blob API로 로컬 저장

---

## 4. 한국어 전처리 방식 (핵심 설계 결정)

### 4.1 형태소 분석을 사용하지 않는 이유
형태소 분석기(KoNLPy, kiwipiepy, 바른 등)는 모두 Python 서버가 필요하여 순수 브라우저 실행이 불가능하다. 대신 **정규식 기반 조사 제거** 방식을 채택한다.

### 4.2 처리 파이프라인
```
원본 댓글
  ↓ 1단계: 텍스트 정제
    - URL 제거 (https?://\S+)
    - 이모지 제거 (Unicode 범위 \u{1F300}-\u{1FAFF} 등)
    - 특수문자 제거 (한글·영문·숫자·공백만 유지)
  ↓ 2단계: 어절 분리
    - 공백 기준으로 토큰 분리
    - 예: "나는 학교를 좋아해" → ["나는", "학교를", "좋아해"]
  ↓ 3단계: 조사 제거 (정규식)
    - 어절 끝에 붙은 조사 패턴을 긴 것부터 순서대로 제거
    - 예: "학교를" → "학교", "나는" → "나"
    - 제거 대상 조사 목록 (우선순위 순):
      에서부터, 로부터, 한테서, 에게서,
      이라도, 이라서, 이지만, 이어서,
      에서, 에게, 한테, 까지, 부터, 처럼, 보다, 마다, 만큼,
      이나, 이랑, 이고, 이며,
      은, 는, 이, 가, 을, 를, 의, 와, 과, 도, 로, 에, 나, 랑, 서
  ↓ 4단계: 필터링
    - 최소 글자 수 미만 제거 (기본: 2글자)
    - 불용어 목록에 포함된 단어 제거
    - 숫자만으로 이루어진 토큰 제거
  ↓ 결과: 분석용 토큰 목록
```

### 4.3 한계 및 교육적 활용
| 상황 | 처리 결과 | 교육적 활용 |
|------|-----------|-------------|
| 명사 ("학교", "친구") | 정확하게 추출 ✅ | 핵심 주제어 분석에 적합 |
| 동사 어미 ("먹어", "먹었어") | 따로 집계됨 ⚠️ | 활용형 차이를 수업 토론 소재로 활용 |
| 신조어·비속어 | 그대로 집계됨 | 언어 변화 탐구 소재 |
| 영어 단어 | 그대로 집계됨 | 외래어 사용 양상 분석 |

> **교사 안내:** 이 도구는 명사 중심 빈도 분석에 최적화되어 있습니다. 동사·형용사의 어미 변화는 정확히 처리되지 않으므로, 이를 수업 중 토론 주제로 활용할 수 있습니다.

---

## 5. 기능 요구사항

### Feature 1: YouTube 댓글 수집

**목적:** 학생이 발급받은 API 키와 영상 URL을 입력하면 댓글을 수집한다.

**상세 기능:**

| 항목 | 내용 |
|------|------|
| API 키 입력 | 우측 상단 설정 아이콘 클릭 → 모달에서 입력, `localStorage` 저장 |
| 영상 입력 | YouTube URL 전체 또는 Video ID 단독 입력 지원 |
| 수집 범위 | 최상위 댓글만 / 대댓글 포함 (라디오 버튼) |
| 최대 수집 수 | 슬라이더로 설정 (100 / 300 / 500 / 최대) |
| 정렬 기준 | 인기순 / 최신순 (YouTube API `order` 파라미터) |
| 진행 표시 | 페이지 번호, 수집된 댓글 수 실시간 표시 |
| 결과 미리보기 | 댓글 내용, 작성자, 좋아요 수, 작성일 테이블 |
| CSV 내보내기 | BOM 포함 UTF-8 (Excel 한글 깨짐 방지) |
| 취소 기능 | 수집 중 취소 버튼으로 즉시 중단 |

**오류 처리:**
- `403`: API 키 오류 또는 할당량 초과 → 구체적 안내 메시지 + 할당량 확인 링크
- `404`: 존재하지 않는 영상 → 안내 메시지
- 비공개 영상 → 안내 메시지
- 댓글 비활성화 영상 → 안내 메시지
- 네트워크 오류 → 재시도 버튼 제공

**YouTube API 호출 방식:**
```
GET https://www.googleapis.com/youtube/v3/commentThreads
  ?part=snippet,replies
  &videoId={videoId}
  &maxResults=100
  &order={relevance|time}
  &key={apiKey}
  &pageToken={nextPageToken}
```

---

### Feature 2: 데이터 전처리

**목적:** 수집된 댓글을 분석에 적합한 토큰으로 변환한다.

**상세 기능:**

**2-A. 정제 옵션 (체크박스로 개별 선택)**
- URL 제거
- 이모지 제거
- 영어 포함 / 제거 선택
- 숫자 포함 / 제거 선택
- 최소 글자 수 설정 (기본: 2)

**2-B. 조사 제거**
- 자동 적용 (기본 켜짐)
- 켜기/끄기 토글 (비교 실험용)

**2-C. 불용어 관리**
- 기본 불용어 목록 제공 및 화면에 표시
  ```
  그냥, 진짜, 정말, 너무, 좀, 막, 되게, 아주, 매우,
  이거, 저거, 그거, 여기, 저기, 거기, 이런, 저런, 그런,
  하지만, 그런데, 그래서, 왜냐하면, 따라서
  ```
- 불용어 추가: 입력 후 Enter
- 불용어 삭제: 태그의 X 버튼
- 불용어 목록 초기화 버튼

**2-D. 결과 미리보기**
- Before 탭: 원본 댓글 (빨간 형광펜으로 제거 대상 표시)
- After 탭: 전처리된 토큰 목록
- 처리된 토큰 총 수, 고유 토큰 수 표시

**2-E. 내보내기**
- 전처리 결과 CSV 다운로드 (원본 댓글 + 추출된 토큰)

---

### Feature 3: 빈도수 분석

**목적:** 토큰 출현 빈도를 계산하고 표로 제시한다.

**상세 기능:**

| 항목 | 내용 |
|------|------|
| 빈도 계산 | 전처리 완료 즉시 자동 계산 |
| 표시 컬럼 | 순위, 단어, 빈도수, 비율(%), 누적 비율(%) |
| 상위 N개 | 드롭다운: 10 / 30 / 50 / 100 / 전체 |
| 정렬 | 빈도수 내림차순(기본) / 가나다순 / 빈도수 오름차순 |
| 검색 | 실시간 검색 필터 (특정 단어 포함 행 표시) |
| 특정 단어 제외 | 테이블 행에서 직접 "제외" 버튼 → 해당 단어 불용어 추가 후 재계산 |
| CSV 다운로드 | 순위·단어·빈도·비율 포함 |

---

### Feature 4: 막대차트

**목적:** 빈도수 상위 N개 단어를 막대차트로 시각화한다.

**상세 기능:**

| 항목 | 내용 |
|------|------|
| 기본 표시 | 상위 20개 단어 |
| 표시 개수 | 슬라이더 (5 ~ 50개) |
| 방향 | 세로 막대 / 가로 막대 전환 버튼 |
| 색상 | 단색(파랑) / 빈도 비례 색상(진할수록 높은 빈도) / 무지개 |
| 툴팁 | 마우스 오버 시 단어명 + 빈도수 + 비율 |
| 축 레이블 | Chart.js 한국어 폰트(Nanum Gothic) 적용 |
| PNG 저장 | `canvas.toBlob()` 으로 고해상도 PNG 다운로드 |

**Chart.js 설정 주요 옵션:**
```javascript
{
  type: 'bar',  // 또는 'horizontalBar'
  options: {
    plugins: {
      legend: { display: false },
      tooltip: { callbacks: { label: (ctx) => `${ctx.raw}회 (${pct}%)` } }
    },
    scales: {
      x: { ticks: { font: { family: 'Nanum Gothic' } } },
      y: { ticks: { font: { family: 'Nanum Gothic' } } }
    }
  }
}
```

---

### Feature 5: 워드클라우드

**목적:** 빈도수에 비례하는 글자 크기로 단어를 시각적으로 표현한다.

**상세 기능:**

| 항목 | 내용 |
|------|------|
| 최대 단어 수 | 슬라이더 (20 ~ 200개) |
| 색상 팔레트 | 블루 계열 / 레드 계열 / 그린 계열 / 무지개 / 모노크롬 |
| 배경색 | 흰색 / 검정 / 연회색 |
| 폰트 | Nanum Gothic (한글 깨짐 없음) |
| 재생성 | 랜덤 배치로 다시 그리기 버튼 |
| PNG 저장 | 캔버스 전체 PNG 다운로드 |

**wordcloud2.js 설정:**
```javascript
WordCloud(canvas, {
  list: tokenFreqArray,       // [단어, 빈도수] 배열
  fontFamily: 'Nanum Gothic',
  color: colorFunction,
  backgroundColor: bgColor,
  weightFactor: (size) => Math.sqrt(size) * scaleFactor,
  minSize: 10,
})
```

> **참고:** wordcloud2.js는 캔버스 크기와 단어 수에 따라 일부 단어가 배치 공간 부족으로 누락될 수 있음. 최대 단어 수를 줄이거나 캔버스를 크게 설정하여 해결.

---

### Feature 6: 소셜네트워크 분석 (SNA)

**목적:** 같은 댓글에서 함께 등장하는 단어 쌍의 관계를 네트워크 그래프로 시각화한다.

**상세 기능:**

**6-A. 공기어 행렬 계산 (JS)**
```
댓글별 토큰 목록 예시:
  댓글1: ["학교", "친구", "재미"]
  댓글2: ["학교", "선생님", "재미"]
  댓글3: ["친구", "재미", "게임"]

공기어 행렬:
  학교-친구: 1, 학교-재미: 2, 학교-선생님: 1
  친구-재미: 2, 친구-게임: 1, 재미-선생님: 1, 재미-게임: 1
```

**6-B. 필터 설정**
| 항목 | 내용 |
|------|------|
| 최소 공기 빈도 | 슬라이더 (2 ~ 10회, 기본: 3회) |
| 표시 단어 수 | 슬라이더 (10 ~ 100개, 기본: 50개) |

**6-C. 시각화 (D3.js force simulation)**
| 요소 | 표현 방식 |
|------|-----------|
| 노드 | 원형, 크기 = 단어 빈도수에 비례 |
| 노드 레이블 | 단어명 표시 (Nanum Gothic) |
| 노드 색상 | 연결 중심성 높을수록 진한 색 |
| 엣지 | 선, 굵기 = 공기 빈도에 비례 |
| 엣지 색 | 반투명 회색 |

**6-D. 인터랙션**
- 드래그로 노드 이동
- 마우스 스크롤로 줌 인/아웃
- 노드 클릭 → 해당 단어가 포함된 원본 댓글 목록 사이드 패널에 표시
- 노드 더블클릭 → 해당 노드 고정/해제

**6-E. 중심성 지표 테이블**
| 컬럼 | 내용 |
|------|------|
| 단어 | 노드 단어 |
| 연결 중심성 | 연결된 단어 수 / (전체 노드-1) |
| 가중 연결 중심성 | 연결된 엣지 가중치 합 |
| 공기어 목록 | 연결된 단어들 |

**6-F. 내보내기**
- 중심성 테이블 CSV 다운로드
- 네트워크 그래프 PNG 다운로드 (SVG → Canvas 변환)

---

## 6. UI/UX 설계

### 6.1 전체 레이아웃
```
┌──────────────────────────────────────────────────────────┐
│  📊 YouTube 댓글 분석기     국어 수업용          [⚙️ 설정] │
├──────────────────────────────────────────────────────────┤
│  ① 수집  →  ② 전처리  →  ③ 빈도  →  ④ 차트  →  ⑤ 워드클라우드  →  ⑥ SNA  │
│  (완료된 스텝은 체크 표시, 미완료 스텝은 비활성)          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                  현재 스텝 콘텐츠                         │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  [← 이전]              수집: 487개 | 토큰: 3,241개  [다음 →] │
└──────────────────────────────────────────────────────────┘
```

### 6.2 설정 모달 (⚙️ 클릭 시)
```
┌─────────────────────────────┐
│ 설정                    [×] │
├─────────────────────────────┤
│ YouTube API 키              │
│ [AIza...________________]   │
│ 💡 Google Cloud Console에서  │
│    발급받는 방법 안내 링크   │
├─────────────────────────────┤
│               [저장] [취소] │
└─────────────────────────────┘
```

### 6.3 스텝별 진행 상태 관리
- 각 스텝의 완료 여부를 JS 상태로 관리
- 이전 스텝 미완료 시 다음 스텝 비활성화 (단, 이전 스텝으로 돌아가기는 가능)
- 페이지 새로고침 시 수집된 데이터 초기화 (localStorage에는 API 키만 유지)
- 스텝 이동 시 데이터 유실 경고 없음 (뒤로 가도 데이터 유지)

### 6.4 반응형 대응
- 데스크탑(1024px 이상): 전체 레이아웃
- 태블릿(768px 이상): 일부 컬럼 축소
- 모바일: 수집·빈도 분석까지만 최적화, SNA는 데스크탑 권장 안내

---

## 7. 비기능 요구사항

### 7.1 성능
| 작업 | 목표 |
|------|------|
| 댓글 수집 (500개) | 10초 이내 (API 응답 속도 의존) |
| 전처리 (500개 댓글) | 1초 이내 |
| 빈도 분석 | 즉시 (< 100ms) |
| 막대차트 렌더링 | 즉시 |
| 워드클라우드 생성 (100단어) | 3초 이내 |
| SNA 그래프 (50노드) | 3초 이내 |

### 7.2 보안
- YouTube API 키는 `localStorage`에만 저장, 외부 서버로 절대 전송하지 않음
- 모든 데이터 처리가 브라우저 내에서 완결됨
- HTTPS(GitHub Pages 기본 제공)로 통신

### 7.3 접근성
- 모든 버튼에 `aria-label` 적용
- 색상 외 다른 시각 요소(아이콘, 레이블)로도 상태 구분
- 한국어 인터페이스 100%

### 7.4 오류 복구
- API 호출 실패 시 재시도 버튼 제공
- 처리 중 오류 발생 시 해당 스텝만 재실행 가능 (전체 초기화 불필요)

---

## 8. 파일 구조

```
youtubecrawling/
├── index.html                  # 진입점, CDN 로드, 레이아웃 구조
├── css/
│   └── style.css               # 커스텀 스타일 (Tailwind 보완)
├── js/
│   ├── app.js                  # 앱 초기화, 스텝 라우팅, 전역 상태
│   ├── settings.js             # API 키 관리 (localStorage)
│   ├── step1_crawler.js        # YouTube API 호출, 댓글 수집
│   ├── step2_preprocess.js     # 정제, 조사 제거, 불용어 처리
│   ├── step3_frequency.js      # 빈도수 계산 및 테이블 렌더링
│   ├── step4_barchart.js       # Chart.js 막대차트
│   ├── step5_wordcloud.js      # wordcloud2.js 워드클라우드
│   ├── step6_sna.js            # D3.js 공기어 네트워크 그래프
│   └── utils.js                # CSV 내보내기, PNG 저장, 공통 유틸
└── assets/
    └── stopwords.js            # 기본 불용어 목록
```

### CDN 의존성 (index.html에서 로드)
```html
<!-- 스타일 -->
<link href="https://fonts.googleapis.com/css2?family=Nanum+Gothic&display=swap" rel="stylesheet">
<script src="https://cdn.tailwindcss.com"></script>

<!-- 라이브러리 -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://cdn.jsdelivr.net/npm/wordcloud2@1.2.2/src/wordcloud2.js"></script>
<script src="https://cdn.jsdelivr.net/npm/d3@7"></script>
```

---

## 9. YouTube API 키 발급 가이드 (학생용 안내 화면 포함)

앱 내 설정 모달에 단계별 안내를 포함한다:

1. [Google Cloud Console](https://console.cloud.google.com) 접속 → 구글 계정 로그인
2. 새 프로젝트 생성
3. 좌측 메뉴 → API 및 서비스 → 라이브러리
4. "YouTube Data API v3" 검색 → 사용 설정
5. 사용자 인증 정보 → API 키 만들기
6. 생성된 키를 복사하여 앱에 붙여넣기

> **무료 할당량:** 하루 10,000 유닛. 댓글 100개 수집 시 약 1~2 유닛 소모. 수업 1회 기준으로 충분함.

---

## 10. 개발 마일스톤

| 단계 | 기능 | 완료 기준 |
|------|------|-----------|
| **M1** | 프로젝트 기본 구조 + 설정 모달 | index.html, 스텝 네비게이터, API 키 저장 동작 |
| **M2** | Feature 1: 댓글 수집 | YouTube API 호출, 테이블 표시, CSV 저장 |
| **M3** | Feature 2: 전처리 | 정제 + 조사 제거 + 불용어 처리, Before/After 뷰 |
| **M4** | Feature 3: 빈도 분석 | 빈도 테이블, 정렬, 검색, 제외 기능 |
| **M5** | Feature 4: 막대차트 | Chart.js 차트, 색상 테마, PNG 저장 |
| **M6** | Feature 5: 워드클라우드 | wordcloud2.js, 팔레트, PNG 저장 |
| **M7** | Feature 6: SNA | D3.js 네트워크, 필터, 중심성 테이블 |
| **M8** | 통합 테스트 + GitHub Pages 배포 | 실제 수업 시나리오로 E2E 테스트 |

---

## 11. 미결 사항 (Open Questions)

| # | 질문 | 영향 |
|---|------|------|
| 1 | 영어 댓글 처리 방식 결정 필요 (포함/제외/별도 분석) | Feature 2 |
| 2 | 스텝 데이터를 `sessionStorage`에 저장하여 새로고침 후에도 유지할지 여부 | UX |
| 3 | 워드클라우드 마스크 이미지(사용자 업로드) 기능 추가 여부 | Feature 5 |
| 4 | 교사용 수업 결과 PDF 내보내기(전체 분석 리포트) 기능 추가 여부 | 추가 기능 |
| 5 | 분석 결과 공유 링크 기능 필요 여부 (URL 파라미터로 상태 인코딩) | 배포 후 |

---

## 12. 용어 정의

| 용어 | 설명 |
|------|------|
| 어절 | 공백으로 구분되는 한국어의 기본 단위. "나는"처럼 단어와 조사가 결합된 형태 |
| 조사 | 한국어에서 어절 끝에 붙어 문법적 관계를 나타내는 요소 (은, 는, 이, 가 등) |
| 토큰 | 전처리 후 분석 단위가 되는 단어. 이 앱에서는 조사를 제거한 어절 |
| 공기어 | 같은 댓글에서 함께 등장하는 두 단어의 관계 |
| 연결 중심성 | SNA에서 한 노드가 다른 노드와 연결된 정도 |
